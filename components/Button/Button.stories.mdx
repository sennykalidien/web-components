import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { select, boolean, text } from '@storybook/addon-knobs';
import { html } from 'lit'
import { Button } from './Button';

export const variant = () => select('Variant', ['primary', 'ghost'], 'primary');
export const disabled = () => boolean('Disabled', false);
export const loading = () => boolean('Loading', false);
export const href = () => text('Link', '');
export const label = () => text('Text', 'Default');
export const target = () => select('Target', ['_self', '_blank'], '_blank');

import { withDesign } from 'storybook-addon-designs'

<Meta
    title="Components/Button"
/>

# Button
This is a button component

## Main
<Story
    name="Button/Knobs"
    decorators={[withDesign]}
    parameters={{
     design: {
       type: 'figma',
       url: 'https://www.figma.com/file/7erkg5bpXTYc7EW97CHaK6/Kooyman-B.V.'
     }
    }}>
  {html`
    <custom-button
        variant="${variant()}"
        ?disabled="${disabled()}"
        ?loading="${loading()}"
        href="${href()}"
        .target="${target()}">
        ${label()}
    </custom-button>
  `}
</Story>

## Primary
<Canvas>
    <Story name="Button/Primary" parameters={{ docs: { source: { code: `<custom-button variant="primary">Primary</custom-button>`}}}}>
      {html`
        <custom-button
            variant="primary">
            Primary
        </custom-button>
        <custom-button
            variant="primary"
            disabled>
            Primary Disabled
        </custom-button>
      `}
    </Story>
</Canvas>

## Ghost
<Canvas>
    <Story name="Button/Secondary" parameters={{ docs: { source: { code: `<custom-button variant="ghost">Ghost</custom-button>`}}}}>
      {html`
        <custom-button
            variant="ghost">
            Ghost
        </custom-button>
        <custom-button
            variant="ghost" disabled>
            Ghost Disabled
        </custom-button>
      `}
    </Story>
</Canvas>

## Disabled
<Canvas>
    <Story name="Button/Disabled" parameters={{ docs: { source: { code: `<custom-button disabled>Disabled</custom-button>`}}}}>
      {html`
        <custom-button
            variant="primary"
            disabled>
            Primary Disabled
        </custom-button>
        <custom-button
            variant="ghost"
            disabled>
            Ghost Disabled
        </custom-button>
      `}
    </Story>
</Canvas>